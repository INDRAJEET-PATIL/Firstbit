//Add the (first and last) digit of a given number? 

#include<stdio.h>
void main(){
	
	int n=31,sum=0,r,q;
	
	while(n!=0){
		r=n%10;
		sum=n/10;
		sum=sum+q;
	//	printf("the sum of first and last digit=%d",sum);
	
	}
	printf("the sum of first and last digit=%d",sum);
	
}